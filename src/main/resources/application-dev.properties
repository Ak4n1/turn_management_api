# Development Profile Configuration

# Database Configuration
spring.datasource.url=jdbc:mariadb://localhost:3306/turn_management
spring.datasource.username=turn_user
spring.datasource.password=turn_user_secure_pass_2024
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# JWT Configuration
jwt.secret=dev-secret-key-not-for-production-change-in-prod-profile
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Rate Limiting Configuration (DEV - Relajado)
rate-limit.enabled=false
rate-limit.environment=dev
rate-limit.login.max-requests=100
rate-limit.login.window-seconds=60
rate-limit.refresh.max-requests=200
rate-limit.refresh.window-seconds=60
rate-limit.forgot-password.max-requests=50
rate-limit.forgot-password.window-seconds=3600
rate-limit.public.max-requests=1000
rate-limit.public.window-seconds=60
rate-limit.authenticated.max-requests=2000
rate-limit.authenticated.window-seconds=60

# Security Configuration (DEV - Permisivo)
security.allowed-origins=http://localhost:3000,http://localhost:4200,http://localhost:8080,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:4200,http://127.0.0.1:8080,http://127.0.0.1:5173
security.cors.max-age=3600
security.cors.allow-credentials=true

# Cookie Configuration (DEV - Localhost)
cookie.secure=false
cookie.http-only=true
cookie.same-site=Lax
cookie.domain=localhost
cookie.path=/
cookie.max-age=604800

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=encabojuan@gmail.com
spring.mail.password=egwk mhss jwav bzza
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
mail.from=encabojuan@gmail.com
mail.from.name=Turn Management System
app.base-url=http://localhost:8080
app.frontend-url=http://localhost:4200

# Logging
logging.level.com.ak4n1.turn_management=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.mail=DEBUG

